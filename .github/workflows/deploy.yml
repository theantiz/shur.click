name: shur.click CI/CD

on:
  push:
    branches: [main]

env:
  ACR_LOGIN_SERVER: shurclickacr.azurecr.io
  IMAGE_NAME: shurclickacr.azurecr.io/shur-click

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Build JAR
        run: mvn package -DskipTests -q
        working-directory: backend

      - name: Log in to ACR via crane
        run: crane auth login shurclickacr.azurecr.io -u "${{ secrets.ACR_USERNAME }}" -p "${{ secrets.ACR_PASSWORD }}"

      - name: Build image
        run: docker build --platform linux/amd64 -t ${{ env.IMAGE_NAME }}:latest -t ${{ env.IMAGE_NAME }}:${{ github.sha }} ./backend

      - name: Push image
        run: |
          docker push ${{ env.IMAGE_NAME }}:latest
          docker push ${{ env.IMAGE_NAME }}:${{ github.sha }}